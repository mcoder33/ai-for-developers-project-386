services:
  - type: web
    name: call-booking
    runtime: docker
    plan: free
    dockerfilePath: ./Dockerfile
    dockerContext: .
    healthCheckPath: /api/public/event-types
    autoDeployTrigger: commit
